feat(g4): --parse-errors 等价落地 — 错误响应原文/上下文/SQL 片段进证据链

- 新增 engine/parseErrors.js 纯函数：extractErrorContext（报错签名+原文窗口截取+body 上限截断）/ extractSqlFragment（PG LINE n:/ MySQL near/ 各库报错片段）
- ErrorDetector：命中返回 body；config.parseErrors===true（opt-in，对标 sqlmap --parse-errors）时写 result.errorDetail（signature/context/sqlFragment/body），证据文本追加错误原文限量片段
- scanRunner：vuln.errorDetail 透传（vuln 对象构造后显式附加）
- CLI：--parse-errors 开关 + help + buildConfig 透传
- REST：KNOWN_CFG_KEYS + pickBool 透传（默认不含键回退 defaults false）
- defaults：parseErrors: false（零行为变化）
- 测试：errorParse.test.js 10 例（纯函数/Detector 集成/CLI/REST）
- 回归：相关套件 35/35 全绿
