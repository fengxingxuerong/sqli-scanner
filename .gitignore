node_modules
# 重构临时脚本/临时文件（不入库）
._*
.tmp-*
dist
server/node_modules
src-tauri/target
src-tauri/binaries
src-tauri/externalBin
*.log
# 环境变量/密钥文件一律不入库（模板 .env.example 除外）——防止 .env.ai 类真实凭据再次误提交
.env*
!.env.example
!server/.env.example
.DS_Store

# scratch / debug artifacts leaked into project dir during dev
/nul
debug-target.mjs
live-scan-demo.mjs
# push-via-api.mjs 的运行产物（日志/结果 JSON），不入库
.push-log*.txt
.push-via-api-result.json
.mock/
*.bak*
.trash/
logs/

# session snapshot (auto-generated by resume, machine-specific, not committed)
sqli-session-latest.json
server/sqli-session-latest.json
server/sqli-session-*.json
server/*.err
# 错误日志产物（根级 + 任意层级）
*.err

# vitest config temp artifacts (leftover on abnormal exit)
vitest.config.ts.timestamp-*.mjs
*.tmp
del-nul*
coverage

# engine build output (sql.js bundled with wasm shim)
server/dist-engine/

# tsc build info
*.tsbuildinfo

# recall-lab run results (auto-generated, machine-specific, not committed)
e2e/recall-lab/results/

# [P2-HYGIENE 2026-09-09] 一次性排障脚本与本地代理临时目录：不属于任何回归入项，
# 但会把 `eslint .` 与 `git status` 搅浑（历史审计会话残留）。统一忽略，需要时自行处理。
e2e/diag/
e2e/waf-real/.sqlmap-waf/
# [2026-09-25] 这里从"整目录忽略"改成"只忽略非 md 产物"。原因：README 把 `npm run waf-real`
# 叫作"对外唯一口径"，可那份报告根本不在仓库里 —— 客户 clone 下来看不到任何数字来源，
# 而扫 md 的产物自洽守卫（server/tests/e2eArtifacts.consistency.test.js）在 CI 的干净
# checkout 里连文件都找不到，等于对这份口径**永远不生效**。同目录的 json 继续忽略。
e2e/waf-real/results/*
# 例外只开给 CRS A/B 那几份报告（README 引它、自洽守卫在 CI 里要能找到它）；
# 同目录其余 md（保真度明细、tamper 现场）仍按原政策留在本地，避免一次改规则引入六份新产物。
!e2e/waf-real/results/waf-real-report*.md
e2e/real-world-lab/results/
e2e/recall-lab/.sqlmap-tmp/

# [P0-FIX] e2e 结果产物入库噪音：sqli-labs/tamper-matrix/waf-lab 的结果文件为每次跑批生成，
# 统一 gitignore + 改为 CI artifact（已有 git 跟踪的历史文件需 git rm --cached 处理）。
e2e/sqli-labs/results/
e2e/tamper-matrix/results/
e2e/waf-lab/results/.git-broken-20260906/

# 红队评测产物（本地生成，不入库）
e2e/redteam-lab/out/
e2e/redteam-lab/sqlmap-out/

# e2e 临时诊断脚本与运行输出（本地 scratch，不纳入版本管理）
e2e/diag/

# [策略扩展 2026-09-13] e2e 运行产物 json 出库（md 摘要/报告保留留痕；历史跟踪文件已 git rm --cached）
e2e/detection-runner/results/*.json
e2e/multi-engine-lab/results/*.json
e2e/oob-real-lab/results/*.json
e2e/pentest-lab/results/*.json
e2e/real-mysql-lab/results/*.json
e2e/real-mysql-lab/.sqlmap-out/
e2e/waf-lab/results/*.json
e2e/redteam-lab/results-*.json

# [P1] Tauri 构建自动生成的 schema（源为 src-tauri/capabilities/default.json，tauri build/dev 时重建）
src-tauri/gen/schemas/

# [P1] UDF 构建产物（udf_sys.c + build-udf.py 已入库，DLL 可重建；避免杀软误报入库）
e2e/udf-lab/*.dll

# [.env 策略对齐] .env* 默认不入库；以下两个为无敏感信息的安全文件（仅 VITE_API_BASE），显式放行与实际跟踪状态一致
!.env.development
!.env.tauri

# agent 临时诊断脚本
**/.openclaw-*.mjs

# [2026-09-18] PG 靶场运行器：上一批次**明确决定不提交**，但文件留在磁盘上备用。
# 记在这里而不只记在会话笔记里，是因为它长期挂在 `git status` 的未跟踪区，
# 每一轮都要重新判断一次「这是产物还是垃圾」。不提交的两条理由：
#   ① 未端到端验证 —— 就绪探测（pg_ready）判定一直失败，且 `PG_DB` 默认值
#      `sqli_lab` 在 pg-smoke 里可能根本不存在（会报 database does not exist）；
#   ② 硬编码 `D:/pg-smoke` 路径 —— 恰好违反同一轮学到的「配置不能依赖环境遗留」。
# 另注：跑它会弹出控制台窗口（本机反感），默认不执行。要跑 PG 系三个套件
# （oob-real-lab / pg-osshell / concurrent-isolation）改用 `npm run lab:redteam`，
# 它的 env.mjs 已经在同进程里把 PostgreSQL 拉起来了。
e2e/run-with-pg.py

# blackbox-lab 独立评测靶场产物（扫描输出 + 依赖目录链接）
e2e/blackbox-lab/out/
e2e/blackbox-lab/node_modules

# acceptance 套件失败时落盘的原始输出（本地排障现场，不入库；文件名见 acceptance.mjs 的 DIAG-FIX）
e2e/results/last-failure-*.log

# 定向 / 跳过 heavy 的验收报告（本地产物，不入库）。
# [2026-09-20] 与它配套的修复：这类运行原先会**覆盖掉**入库的 acceptance-report.md，
# 于是库里那份「12 PASS」全量结论被一次 `--only=file-write` 的「1 PASS」替换掉 ——
# 实测发生过（现场报告只有一行 fileWrite）。现改写到 .partial.md 并在此忽略，
# 全量报告只由真全量运行写入。判据在报告头部（套件范围 N/M），不依赖文件名。
e2e/results/acceptance-report.partial.md

# 扫描台账运行时数据（不入库）
# [2026-09-18] 这条规则从 a65839b 起就**空转**着：加规则时没配套 `git rm --cached`，
# 于是 index.jsonl 与两份扫描目录一直挂在版本控制上，每跑一次扫描就多一行 diff
# （本轮实测工作区里因此堆了 237 行台账改动，和这批改动的主题混在一起看不清）。
# 现已补上 untrack（文件留在磁盘上，不丢数据）。留这条注释是为了不再退回去：
# 台账是**追加式**的扫描历史，写进去的就是扫过的目标地址 —— 哪天扫了客户系统，
# 这些地址会直接进 git 历史。ledgerDir() 每次都会 mkdirSync，listScans() 也容忍
# index.jsonl 不存在，所以新克隆没有这个目录一样能跑。
server/data/ledger/

# [2026-09-17] 一键扫描产物：scripts/one-click-scan.mjs 默认输出到 reports/<host>-<时间戳>/，
# 每次扫描一个目录、含可能敏感的目标地址与拖库数据，必须忽略。
reports/

# [2026-09-17] 利用能力验证产物（fileRead/fileWrite 落地文件与断言快照）
e2e/fileops/results/

# [2026-09-18] UDF 验证沙箱运行时产物
# .mysql-sandbox/ 是「用完即毁」的隔离 MySQL 实例（datadir + 二进制日志 + 错误日志），
# 属本地运行时数据，含机器路径，不入库；由 mysql_sandbox.py --init 按需重建。
e2e/udf-lab/.mysql-sandbox/
e2e/udf-lab/__pycache__/

# [2026-09-18] PG 靶场运行器（e2e/run-with-pg.py）的就绪探针生成物。
# 每次运行按内联模板落盘，属派生文件，不入库。
e2e/.pg-ready-probe.cjs

# 本机工作记忆（WorkBuddy 会话上下文，非项目源码，不入库）
# [2026-09-18] 规则加在 e97082d，但 `memory/2026-09-09.md` 是在 6f7b728「对象库损毁后
# 重建基线」那次恢复快照里被顺带扫进来的，一直跟踪到今天（同一批未配套的还有
# server/data/ledger，见上文）。现补 `git rm --cached`：文件留在磁盘，只是不再入库。
.workbuddy/
# [2026-09-26] Agent 运行时目录与宿主产物标记（本机专属，不入库）
# 三者都是"跑起来就有、与源码无关"的东西，此前长期挂在 `git status` 的未跟踪区，
# 每轮都要重新判断一次"这是产物还是垃圾"——与 .workbuddy / e2e/run-with-pg.py 同源处置。
# 已核实：这三个路径下**没有任何被 git 跟踪的文件**，故只需加规则，无需 `git rm --cached`。
#   · .box-agent/            —— 宿主（Box-Agent）的会话/任务注册表运行时目录
#   · .box-agent-scratch/    —— 运行时保留的临时区，由 runtime 自行安全回收，不手动 rm
#   · *.artifact.json        —— 宿主为已发布产物生成的标记文件（本仓无入库先例）
# 用 `*.artifact.json` 而非逐文件列举：它是宿主对**任意**交付物都会生成的东西，
# 逐文件写会随交付物增减而腐烂（同族教训：登记表只增不减会变噪声源）。
.box-agent/
.box-agent-scratch/
*.artifact.json

# ─────────────────────────────────────────────────────────────────────────────
# [2026-09-26 修复记录] 上文 5 行中文注释曾损坏为字面 U+FFFD（共 167 个替换字符）
#
# 根因：这些注释在某次写入时用了 **GB18030** 编码（不是 UTF-8），而仓库其余部分
# 是 UTF-8。严格按 UTF-8 读那几行会得到替换字符 —— 由于是**已落盘的字面**
# `EF BF BD`，任何后续转换都救不回来（git 的 19 个历史版本里同样是替换字符）。
#
# 修复方式：**不是按大意重写，而是从 git 历史里精确还原原文**。原始 GB18030 字节
# 仍完整保存在 blob 中，用 GB18030 解码即得原句；还原后逐行做了 ASCII 骨架比对
# （当前损坏行 vs 历史候选行，5/5 一致）以排除张冠李戴。
#   还原来源：3627ae3「chore(repo): 运行产物与构建生成物出库 + .gitignore 策略对齐」
#   涉及行：e2e/diag 注释、[策略扩展 2026-09-13]、[P1] Tauri schema、[P1] UDF、
#           [.env 策略对齐]
# 原文中的可验证事实已逐条复核：.env.development/.env.tauri 确仅含 VITE_API_BASE；
# udf_sys.c / build-udf.py 确已入库；3627ae3 提交日期确为 2026-09-13。
#
# ⚠️ 遗留（未在本轮处理）：line 54 与 line 78 的 `e2e/diag/` 是**重复规则**。
# 两次分别由 P2-HYGIENE 与本次还原的注释各自引入，语义相同、无行为差异。
# 合并会改变 diff 归属、且可能踩到 blame 追溯，故只记录不动。
# ─────────────────────────────────────────────────────────────────────────────
