// ============================================================================
// scan/constants.js —— 扫描流水线共享常量
// （原 scanRunner.js 顶部私有常量；detect 段封边后 detect.js 也需要，收拢到此单一来源）
// ============================================================================

// 暂停轮询间隔（ms）：扫描暂停时在点边界阻塞等待，避免忙等
export const PAUSE_POLL_MS = 300;
// HTTP 请求日志节流间隔（ms）：同一扫描内 http_request 事件最少间隔，防 SSE 高峰期刷屏
export const HTTP_LOG_THROTTLE_MS = 250;
