# 待办盘点（2026-09-12）

> 来源：代码/文档全文扫描（55 处未完成标记）+ 抽样核实历史审计条目的当前状态。
> **不靠记忆罗列**——每条都标了核实方式与现状。

## A. 能力边界（已对外声明，属定位选择）

| 项 | 现状 | 声明位置 |
|---|---|---|
| 11 个模板库无真机验证 | SQL Server / Oracle / TiDB / DM8 / ClickHouse / DB2 / Sybase / Firebird / Informix / Access / MonetDB | README 三档分层（⛔）+ 报告 `summary.dbmsEvidence.caveat` |
| 后渗透能力 | `--os-pwn`（反弹/meterpreter/MSF）、OLE Automation（`sp_OA*`，GPL 约束）仅桥接 | `docs/vs-sqlmap-analysis/08-*` 标"真·未实现" |
| 利用链 | `fileRead`/`fileWrite` 真闭环；UDF / os-shell 标实验特性、未真机验证 | README 利用能力实测口径 |
| 认证 | 仅 Basic / Digest；**NTLMv2 / Negotiate(Kerberos) 不支持** | `core/ntlmAuth.js:22` 注释 |
| 内联拖库 | 逐表内联提取未实现（复用 UNION / 字节级盲注通道） | `detectors/InlineQueryDetector.js:13` |
| 方言版本差异 | 仅识别 DBMS 版本，不按版本切 payload | `docs/vs-sqlmap-analysis/08-*` P2-2 |

**这些不是"欠债"**，是能力边界；README 已逐条声明，客户读报告能看到。

## B. 技术债（有记录、部分仍缺）

| 项 | 状态 | 核实方式 |
|---|---|---|
| **集合运算 / COLLATE 提取模板缺失** | ❌ **仍缺** | 刚核实：`extractionMaps.js` 有 ORDER BY，**无 INTERSECT / EXCEPT / COLLATE** |
| `oobTrigger` 预留开关 | ❌ 仅预留未实现 | `config/defaults.js:276` 注释"本期未实现" |
| DBMS 版本分支（运行时切 payload） | ❌ 未做 | 同上 A 表 P2-2 |
| tamper 幂等声明不实 | ✅ **已修**（9-09） | `blindbinary.js:76-78` 已改正并加注释；有 `tamper.idempotency.test.js` 守卫 `f(f(x))===f(x)` |
| `--invalid-*` 系列缺失 | ✅ **已实现** | 代码有 `applyInvalidValues(pointsToScan, cfg)`（`scan/discover.js`） |
| 已知注入点直通入口 | ✅ **已实现** | `applyKnownPoints` + `config.knownPoint` |
| CORS / Sec-Fetch 守卫 | ✅ **已修** | `server/index.js` 有 `Sec-Fetch-Site` 判定 + `api.csrfGuard.test.js` |
| `detect` 段 37 个散变量 | ⏳ 需立项 | `docs/runScanLoop-拆分方案-2026-09-12.md`（纯搬移收益不抵风险；根治需状态收拢） |

> 注：9-08 的审计清单里，#13~#20 多数已在后续批次修掉（抽样核实 #14 MySQL 报错族已补
> `floor(rand(0)*2)` / `GTID_SUBSET`，仅 `json_object_agg` 系仍缺；#18 MSSQL 中文 HEX 往返
> **未能核实**——需 MSSQL 环境）。

## C. 工程 / 清理

| 项 | 说明 | 建议 |
|---|---|---|
| `.git-broken-20260906/` | 33 文件 / 0.3MB，git 事故残留 | 确认后删 |
| `coverage/` 1.8MB、`logs/` 1.2MB、`deliverables-demo/`、`.mock/` | 构建/运行产物 | 确认是否已 gitignore；未跟踪则删 |
| `D:/mysql/data-backup-20260912/` | 187MB，我误判 InnoDB 损坏时建的备份（**数据实际未损坏**） | 经确认后可删 |
| 单测分层（快/慢） | 全量 1735 用例约 5 分钟 | 改代码只跑快测可省时间 |
| e2e 靶场物理整合 | 仍 17 个目录（已做统一入口 `e2e/run-all.mjs`） | 物理合并收益低，可不做 |

## D. 建议优先级

1. **`COLLATE` / 集合运算提取模板** —— 实战价值最高：MySQL collation 不匹配（`Illegal mix of collations`）
   是 UNION 注入第一高频报错，缺模板等于放弃这批注入点。有明确验收方式（靶场加 collation 靶点）
2. **清理 C 段遗留物** —— 零风险，10 分钟
3. **定位钉死** —— 决定 A 段哪些该补（走"实战交付"就该补利用链；走"检测为主"则现状够）
4. **`detect` 状态收拢** —— 需单独立项设计，别顺手做
