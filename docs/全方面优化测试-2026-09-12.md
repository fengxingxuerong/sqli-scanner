# 全方面优化测试报告（2026-09-12）

> 执行：AutoClaw（渗透工程师视角）· 基线 master@6757d1a → 修复后 e6c1688
> 范围：全量 e2e 靶场基线复跑 → 失败根因修复 → 全量回归确认

---

## 一、结论

| 指标 | 优化前 | 优化后 |
|---|---|---|
| run-all 靶场 | 10/13（3 个失败 + 1 个假 PASS） | **13/13 全部通过** |
| redteam-lab（19 漏洞点 + 7 安全对照） | 0/26 hit（假 PASS，靶场根本没起） | **19/19 命中、0 误报** |
| detection-runner | 17 PASS / 2 FAIL / 3 WARN | **19 PASS / 0 FAIL / 0 WARN** |
| real-mysql-lab | 0.6s 即失败（连 3307） | **10/10 PASS** |
| udf-lab（UDF 接管真机验证） | EBUSY 失败 | **PASS（osShell=ok 真实回显）** |
| exploit 相关单测 | — | 48/48 |

过程中挖出并修复 4 个真实缺陷 + 1 个编排缺陷，全部有真机复现证据。

## 二、环境重建（复现前提）

| 组件 | 动作 |
|---|---|
| MySQL 8.0.28 | D:\mysql（服务未注册），用最小配置拉起：只绑 127.0.0.1:3306、secure_file_priv=""（fileRead/fileWrite/UDF 验证需要）。配置存于 D:\tmp-sqlilab\my.ini。凭据 root/root |
| PostgreSQL | D:\pg-smoke\data，管理员身份禁止启动 postgres → 用本地低权账号 CodexSandboxOffline + 目录 ACL 放行后拉起 5432 |
| 靶场依赖探测 | node e2e/run-all.mjs --list → 13/13 就绪 |

## 三、缺陷清单（按发现顺序）

### 1. redteam-lab 编排缺陷（run-all 假 PASS）——最典型
- 现象：run-all 报 redteam-lab 通过（41.8s），但提示行是 r2 done: 0/26 hit——19 个注入点全部漏检。
- 根因：run-scan.mjs 只负责扫描，不含起靶场逻辑（需要 env.mjs 常驻拉起 MySQL+PG+靶场）。run-all 直接把 run-scan 当入口，靶场没起 → 全部连接失败；而 run-scan 依然 exit 0，run-all 只看退出码 → 假 PASS。这正是 acceptance.mjs 注释里说的「中间层自报成功、无人校验外部事实」。
- 修：新增 e2e/redteam-lab/run-with-env.mjs 编排包装：先常驻拉起 env.mjs → 靶场端口就绪作为硬前置（90s 超时即失败）→ 运行 run-scan → finally 杀 env。run-all 的 redteam-lab entry 指向 wrapper。
- 修复后：19/19 漏洞点命中 + 7 安全对照 0 误报。

### 2. Exploiter.myOsShell 误诊「UDF 未注册」（真机实测）
- 现象：udf-lab step6 osShell("whoami") 返回 ok:false + "UDF 未注册，建议调用 udfInstall 后重试"，但同一注入点直调 extractScalar("SELECT sys_eval(""cmd /c echo X"")") 能拿到回显——UDF 明明已注册。
- 根因：中文 Windows 的 whoami 输出含 GBK 原始字节；CAST(... AS CHAR) 无法字符化 → 整个标记表达式得 NULL → 回显列是字符串 NULL 而非 __S__..__E__ → 路径1 判空 → 直接跳到路径3「未注册」结论。把「输出不可字符化」误诊为「未注册」，会把调用方引向重投 DLL 的错误处置方向。
- 修：Exploiter.js myOsShell 新增路径 2.5——用确定 ASCII 输出的 cmd /c echo <随机数> 探测：echo 命中 → 返回专用诊断（已注册、命令已执行、输出不可字符化、suggestHexEcho: true 指引 hex 编码回显）；echo 也不命中 → 才是真正未注册。
- e2e 同步：step6 命令从 whoami 换成 cmd /c echo osshell_ok（whoami 的 GBK 输出不可字符化是环境事实不是引擎缺陷），step6 现在验证的是 osShell 真实 ok 路径。

### 3. recall-lab mock 求值器未跟进引擎 WRAP 升级
- 现象：detection-runner 的 union_extract / extract_mock_values 2 FAIL——databases/tables 提取全空。
- 根因：引擎 2026-09-12 的 WRAP 修复（commit 07a1851）把 MySQL 家族标记包裹升级为 CONVERT(CONCAT(...) USING utf8mb4) COLLATE utf8mb4_bin（修 UNION 混 collation 报错），但 recall-lab 的 SQL 求值器不识别 CONVERT(... USING ...) 与 COLLATE 尾巴 → 求值落 unknown → 回显问号 → 提取判空。引擎与靶场演进不同步的典型。
- 修：lab-server.js evalItem 加两个分支：convert（剥 USING cs / 类型参数后对主表达式求值）+ 顶层 collate 尾巴剥离（排序规则声明不改变标量值）。
- 修复后：19/19 PASS / 0 FAIL / 0 WARN（连之前 3 个 WARN 的请求数超限都归零——提取不再浪费探测请求）。

### 4. real-mysql-lab 端口漂移（F4 修复漏网）
- 现象：0.6s 失败，ECONNREFUSED 127.0.0.1:3307。
- 根因：2026-09-10 F4 已把 init-db/lab-app/verify 的默认端口 3307→3306，但 verify.mjs L25 漏改。
- 修：统一 3306。修复后 10/10 PASS（noisy 首跑 10.3s 只出 time、二跑起 5.8s 稳定出 boolean——冷缓存噪声自适应首轮偏慢，属预期非回归）。

### 5. udf-lab DLL 投递 EBUSY（环境残留，非代码缺陷）
- 现象：copyfile udf_sys.dll → plugin_dir 报 EBUSY。
- 根因：上一轮测试残留的 udf 函数已注册，mysqld 进程加载了 DLL 文件句柄锁死。
- 处置：DROP FUNCTION → 重启 mysqld 释放句柄 → 清理残留 DLL → 复跑即 PASS。此问题对新人复现是隐形坑。

### 6. 附带发现（记录，未修）
- redteam-lab 靶场 path 参数对 %%22 双重解码抛 URIError → 500。是靶场健壮性瑕疵（引擎正确按异常响应处理），非引擎缺陷。
- 默认发包参数激进（ratePerSec 50 / concurrency 4 / timeout 5s）仍未调——属评审 P1 项，涉及行为契约与大量测试基线，建议单独立项。

## 四、修复后全量回归（最终态）

node e2e/run-all.mjs → 13/13 通过

- redteam-lab        65.4s   r2 done: 19/26 hit（19/19 漏洞点 + 0 误报）
- retest-lab          2.0s   单点重测断言通过
- multi-engine-lab   58.1s   H2/HSQLDB/Derby 真 JDBC，安全对照零误报
- tamper-matrix       0.3s
- real-world-lab     22.5s   second_order 端到端检出
- real-mysql-lab     30.6s   10/10 PASS
- pentest-lab        27.4s   漏洞场景 10/10、安全误报 0
- waf-real            0.1s   CRS A/B：off→on 技术位提升（waf-verify 单独复跑确认）
- oob-real-lab       14.8s   OOB 带外全链路回连命中
- recall-lab          5.9s   安全靶场零误报
- detection-runner   24.1s   19 PASS / 0 FAIL / 0 WARN
- udf-lab             2.2s   UDF 接管 osShell=ok
- waf-lab            10.3s   detectRateB(100%) > detectRateA(0%)

waf-real 主验证（waf-verify.mjs 单独跑）：CRS 4.1.0 下 tamper off → on，
num 1/3→2/3、str/like/orderby 0/1→2/1、blind 1/1→2/1，安全对照误拦 0。

## 五、复现方式（下次接手）

```powershell
# 1. 拉起 MySQL（secure_file_priv 放行是 fileRead/UDF 验证硬前提）
D:\mysql\bin\mysqld.exe --defaults-file=D:\tmp-sqlilab\my.ini --console
#    凭据 root/root；sqli_lab 库已存在（users 5 行）

# 2. PostgreSQL（OOB 靶场需要）——必须用非管理员账号
#    目录 ACL 已放行：icacls D:\pg-smoke /grant "CodexSandboxOffline:(OI)(CI)F" /T
#    账号密码 sandbox；见 run-with-env.mjs 同款 Start-Process -Credential

# 3. 全量回归
$env:MYSQL_PASSWORD='root'
node e2e\run-all.mjs
```

## 六、剩余待办（下一批建议）

1. P0：默认发包参数保守化（ratePerSec 50→10、timeout 5s→15s）——需同步更新测试基线，建议独立批次
2. P0：MSSQL/Oracle 真机验证（本机无 Docker，需安装或找远程实例）
3. P1：集合运算（INTERSECT/EXCEPT）提取模板——待办盘点 D.1 剩余部分
4. P2：redteam-lab 靶场 path 参数双重解码 URIError 修复
5. P2：仓库清理（backup/coverage/logs 残留，待办盘点 C 段已列）