# 优化空间评估（2026-09-26）

> **核实方式**：读码 + `git log` + 门禁**退出码**实测 + 复用仓库既有探针。
> 未起任何常驻服务、未跑任何扫描（沿用本仓铁律）。
> 工作区：`32fab83`，**dirty**（2 个已改 + 2 个新增测试文件未提交）。
>
> **为什么必须重做一份**：本仓已有 `docs/项目评价-2026-09-23.md` 与 `docs/优化空间勘查-2026-09-23.md`，
> 但 09-23 之后又有 **40+ 提交**。本仓自己的教训写得很清楚 ——「文档现状段在下一批次后即可能过期」。
> 本文对每条结论标 `[实测]`（本次亲自跑出，附命令/退出码）/ `[读码]`（`文件:行号` 可复核）/ `[推断]`（未实测）。

---

## 〇、先纠偏：三条「仍然挂着」的结论已经过期

照 09-23 的评估开工，这三件事会白做：

| # | 09-23 的结论 | **2026-09-26 实测事实** | 证据 |
|---|---|---|---|
| 1 | **P0-1「CI 全红（账单/额度）」= 第一约束** | **已解除**。远端 CI 已恢复真跑：09-25 `run #73 success`、`run 35902080884` **14 PASS / 0 FAIL**（CRS A/B off=8 on=8、红队 19/19、误报 0）；09-26 `run #75 in_progress` | `.workbuddy/memory/2026-09-25.md:3`、`2026-09-26.md:6,58` |
| 2 | **P0-2「payload 声明式迁移在制品未收口」** | **该批已收口**。`payloadRegistry.js` **983 行 / 167.2 KB → 276 行 / 16.5 KB**，数据搬到 `payloads/registry.json`（153.7 KB），`arch-guard` 已把 `.json` 纳入扫描并登记基线 | `wc -l` 实测；`scripts/.arch-baseline.json` 的 `__size_bytes__` |
| 3 | **P1-6「T1/T2 未接线」「A3 未做」「攻击路径图未实现」「C3 断点续拖是缺口」** | **四项全部落地**。① `chainVerify.js:21` 已 `import { buildCandidateChains } from './bypass/searcher.js'`；② `core/waf/channelPolicy.js` 存在且 `scan/detect.js:19,546-584` 真消费；③ `services/reportAttackPath.js` 存在；④ C3 已于 09-26 更正为「已实现」 | `grep` 实测；`docs/待办全景与执行分解-2026-09-22.md` §七 |

> 教训与仓库第四次同源病例一致：**「某文件里没有 X」≠「项目里没有 X」**，核实前先确认自己看的是正确的承载文件。

---

## 一、P0 · 立刻可做（不依赖任何外部条件）

### 1. 收口在制品 —— `facts:check` 当前红 `[实测]`

```
$ npm run facts:check ; echo $?
1
[facts] ⚠ 采集源已改动 —— _facts.json 可能过期
[facts]   指纹采集于 2026-09-25T19:20:23+00:00（323 个文件）→ 现在 325 个
[facts]   新增（2）：server/tests/extractScope.modes.test.js
                    server/tests/finalize.report.test.js
```

未提交内容：`scripts/mutation-check.mjs`（+2 个变异目标）、`server/src/engine/scan/finalize.js`（blockPolicy 文案修正）、
两个新测试文件。**这批本身质量不错**（mutation TARGETS 从 11 → 13，把 `extractScope.js` 449 行与
`finalize.js` 188 行这两个「零测试挂载」的模块纳入变异门禁），只是没走完收口流程。

**修法**：`git add` → `node scripts/facts-sync.mjs --refresh --coverage` → `--fix` → 提交。
⚠️ `--refresh` 会实跑 `test:server`（spawn Express 实例），按本地铁律**先问一句**。

### 2. 前端覆盖率余量只剩 **0.73pt** —— 门禁架在刀尖 `[实测]`

这是本次**新发现**，且方向是坏的：

| 维度 | 实测 | 阈值 | 余量 |
|---|---|---|---|
| statements | 88.73 | 88 | **0.73** |
| lines | 88.73 | 88 | **0.73** |
| branches | 79.88 | 77 | 2.88 |
| functions | 68.01 | 67 | 1.01 |

对比 09-23 的 2.5pt，余量已缩到 **1/3**。而 09-18 的教训原文写着：
「旧阈值 90 压在实测 90.01 上（差 0.01pt）—— **门禁架在刀尖：既容不下正常波动，也防不住真回退**」。
现在 0.73pt 正朝同一个形状滑回去。服务端侧余量 6–8.5pt，健康，无需动。

**注意区分**：`functions` 余量低是**设计意图**（`vitest.config.ts:64-73` 有完整论证：剩余未覆盖函数 69/86 是 JSX 内联箭头，
按 stmts 规则跟涨等于为样板写测试）。但 `statements`/`lines` 的 0.73pt **不是**意图，是漂移。

**归因已完成（本轮实测 `coverage/coverage-final.json`，44 文件 / 6238 语句 / 未覆盖 703）**：

| 未覆盖语句归属 | 条数 | 占比 |
|---|---|---|
| JSX / 样板 | 460 | 65.4% |
| **真逻辑** | **243** | **34.6%** |

未覆盖「真逻辑」TOP：`ScanConfigPanel.tsx` 69 · `DbTree.tsx` 32 · `ScanWizard.tsx` 19 ·
`useEvents.ts` 18 · **`ValidityBanner.tsx` 13** · `VulnDetail.tsx` 13 · `ReportPage.tsx` 12 · `ScanPage.tsx` 12。

**⇒ 结论：补测这条路走得通，不必降阈值。**
补到余量 ≥2pt 需再覆盖 **125 条**（= 2% × 6238），而真逻辑池有 **243 条** —— 余量充足。
这与 09-18 那次 `func` 的情形**性质相反**（那次是「数学上做不到，除非去测 JSX 样板」，
所以刻意不跟涨）；这次是**做得到**，故首选补测，把阈值下调作为次选。

其中 `ValidityBanner.tsx` 的 13 条最该优先：它是**可信度守卫的 UI 出口** ——
`classifyVerdict()` 的 `no_vulnerability_detected` / `negative` / `hit` / `null` 四分支里，
前三支全未覆盖。这是「被封 / 目标挂 / 会话失效」时用户**唯一能看到的提示**，
改坏了不报错、只会让报告与界面互相矛盾（与 09-18 补测 `renderValidity` 同源，属复发）。

### 2b. 补测已落地（2026-09-26 本轮实测）

新增 3 个测试文件、**34 条用例**，覆盖率 **88.73 → 90.97**（余量 0.73 → **2.97pt**，超过 2pt 目标）：

| 文件 | 用例 | 靶点 | 覆盖变化 |
|---|---|---|---|
| `src/tests/validityBanner.test.tsx` | 16 | `resolveValidityMode` 四分支 + 兼容推断 + 三种 mode 的 severity/文案 | **0 → 97.29%**（branch 92.85 / func 100） |
| `src/tests/scanPage.startFailure.test.tsx` | 9 | `doStart` catch 的四类错误归因 + `parseJson`/`parseForm` 护栏 | 92.11 → **100%** |
| `src/tests/vulnDetail.poc.test.tsx` | 9 | `PocSection`/`CopyBox`（交付五要素里「利用证明」的 UI 出口） | 63.38 → **100%** |

**门禁终态**：`vitest` 50 文件 / **384 passed / 0 fail**；`tsc --noEmit` 0 错；
`arch:guard` / `refs:check` / `eslint`(3 新文件) 全 exit 0。

⚠️ **`facts:check` 在补测当下是 exit 1，收口后已回绿**：本轮新增 3 个测试文件 ⇒ 采集源指纹
从 325 继续增长，需走完收口批次（§1）才回绿 —— 它红的**原因变了**（不再是「payload 迁移未收口」，
而是「本轮新增测试未纳入采集」），性质相同（在制品未收口），处置路径也相同。
**收口结果见下方「收口与 CI 验证」：`facts:check` exit 0 + CI run #76 success。**

> 补充：本批**只新增测试文件，未改动任何生产代码** —— 三个被测源码文件
> （`ValidityBanner.tsx` / `ScanPage.tsx` / `VulnDetail.tsx`）经 `git diff --quiet` 逐个确认 **clean**。
> 即：本批是纯「加断言」，不存在行为变更风险。

#### 收口与 CI 验证（2026-09-26 本轮完成）

已按「先 `git add` → 再 `--refresh`」的顺序收口（**顺序不可颠倒**：`facts-sync` 的指纹采自
**git 索引**而非磁盘，先 refresh 会把新用例算进数字却漏进指纹 ⇒ 本地绿、CI 红）。
commit `8d07c30`（11 files / +1586 −24）：

| 步骤 | 结果 |
|---|---|
| `git add` | 3 个新测试 + 上一批在制品 + `README`/`_facts.json`/`_facts.sources.json` |
| `facts:refresh` | 前端 384 用例/50 文件；服务端 2444（2441 pass / 0 fail / 3 skip）；指纹 **328** 文件 |
| `facts:fix` | 修正 README **8 处**（徽章 2751→2825、覆盖率 88.73→90.97、复测日期 09-25→09-26…），CRLF 保持 741/741 |
| `facts:check` | ✅ **exit 0** |
| 推送 | `push-via-api`（git 协议本网络不通）→ 远端 `eacd5af`，**本地 1430 / 远端 1430 条目，差异 0** |

**CI run #76 = `success`**（11 job success + 1 条件 skipped），关键三项：

- ✅ `lint` 的 **「文档数字口径（README ↔ docs/_facts.json）」** —— 即本轮要修的那个门禁，
  在真实 Linux checkout 下确认通过（**本地绿不代表 CI 绿**，本仓自己的口径如此）
- ✅ `test-server` 含 **`Mutation gate (assertion sensitivity, quick tier)`** —— 本批新增的
  两个变异目标（`extractScope.js` / `finalize.js`）在 CI 真跑
- ✅ `acceptance` 12 套靶场全过（含真 MySQL 的 WAF A/B、recall-lab 18 条、随机化真值电池）

⚠️ **收口时拦下一个真实风险**：`finalize.report.test.js` 的 ⑩ 用例断言
`errorOnlyPoints=2` 且 `doesNotMatch(/blockHits=/)`，**依赖**同批的 `finalize.js` 标签修正。
实测把 `finalize.js` 还原成 HEAD 版后跑该文件 → **1 条红**（实际输出 `blockHits=2`）。
⇒ 只提交测试文件会让 CI 必红，两者必须同批提交（已同批，22/22 绿）。

#### 缺陷注入复验（9 组，逐组恢复源码并确认 `git diff` 干净）

| # | 注入 | 结果 |
|---|---|---|
| 1 | 删掉 `verdict==='inconclusive'` 最高优先分支 | **2 红**（A2 + B1） |
| 2 | 不可靠的阴性从 `inconclusive` 改成 `negative`（最危险的误读） | **1 红**（A5） |
| 3 | `suggestBackoffMs` 改用真值判断（吞掉合法的 0） | **1 红**（B5） |
| 4 | 顶层 `validity` 与 `summary.validity` 优先级反转 | **1 红**（A6） |
| 5 | 删掉旧报告兼容短路 `if (!validity && !verdict) return null` | ⚠️ **存活 —— 等价变异** |
| 6 | ENGINE_BUSY / RATE_LIMITED 合并成一类 | **1 红** |
| 7 | 删掉 `else if (e instanceof Error)` 分支 / `starting` 不复位 | 各 **1 红** |
| 8 | `raw` 与 `curl` 变成二选一 | **2 红** |
| 9 | 复制失败仍标记"已复制"（失败被读成成功） | **1 红**（P7 断言②） |

**注入 5 的存活已归因，不是断言缺口**：用 17 组输入逐一比对两版 `resolveValidityMode` 输出，
**差异 0** —— 末尾的 `if (validity)` + `return null` 兜住了同一语义，该短路行是「冗余但更可读的
早期退出」。已按本仓纪律**不为它编造断言**，并在用例注释里写明（编了也只是测实现细节）。

#### 两处诚实边界（已写进用例注释，避免后人误读）

1. **P7 的敏感度来源**：去掉实现里的 `try/catch` 后，该用例**仍会绿** —— 因为 `onClick` 不 `await`
   `handleCopy`，逃逸的 rejection 被 **vitest 的 Unhandled Errors 机制**接走（套件 exit=1），
   不是被断言抓住。**注入确实被拦住了，但拦它的是 vitest 兜底**。已改为同时钉住两件可观测的事
   （组件不崩 + 失败不得被读成成功），后者经复验**断言本身就能杀**。
2. **`poc: {}` 会渲染一个只有标题的空壳 Paper**（`VulnDetail.tsx:233` 的 `vuln.poc &&` 对空对象为真）。
   真实报告里 `pocBuilder` 不产出这种对象，故属**低危观感瑕疵、不是判据错误** ——
   已用 P2 **登记事实**（而非断言理想行为）。若要收紧，判据应改为 `vuln.poc?.curl || vuln.poc?.raw`。

> 顺带记录三个**测试自身的坑**（都靠实测暴露，不是推测）：
> ① `getByPlaceholderText(/http/i)` 命中多个元素 ⇒ 必须用 `getAllBy...[0]`（与既有测试对齐）；
> ② 同一用例内连续 `render` 会累积 DOM ⇒ 辅助函数里先 `cleanup()`，否则 `findByRole('alert')`
> 会把上一次的文案当成这一次的；③ MUI `Collapse` 默认**保留**子节点、且折叠有 transition
> ⇒ 收起断言必须 `await waitFor(not.toBeVisible())`，同步断言 `toBeNull()` 会假红。

### 3. `getHeader` 三份独立实现未收敛 `[读码]`

```
server/src/core/scanValidityGuard.js:81   export function getHeader(headers, key)
server/src/core/waf/blockPolicy.js:29     function getHeader(headers, key)
server/src/core/waf/WafIdentifier.js:8    function getHeader(headers, key)
```

header 大小写不敏感查找是「WAF 判定」与「扫描有效性判定」的**共同前置**。
三份任一处裁剪/大小写规则漂移 → 同一响应在三处结论不一致。改动小、风险低，
但本仓最擅长的正是这类「同一设施多份、其中一份是旧版」的守卫（§A1 `stripEchoedPayload` 已按同一模式收敛过一次）。

---

## 二、P1 · 高价值（多为产品决策或需靶场）

### 4. UI 键缺口 **66 个**，且从 65 反增了 1 `[实测]`

| 项 | 数 |
|---|---|
| 后端 `KNOWN_CFG_KEYS` | **111** |
| 前端 `SCAN_CONFIG_KEYS` | **45** |
| 实际缺口 | **66** |
| `KNOWN_MISSING_UI_KEYS` 登记数 | **66**（✅ 三条守卫全绿：无未登记缺口 / 无腐烂 / 无虚胖） |

**守卫本身是健康的**（我实测跑了三条反向断言，全部一致）。
但两件事值得注意：
- **缺口在扩大**：09-23 是 65，现在是 66 —— 后端加了键，前端没跟。守卫只能「让缺口可见」，不能阻止它变大。
- **其中混着两类性质完全不同的键**，不该一视同仁：
  - **能力缺失类**（关着 = 少测一整类注入面 = 假阴性）：这是最贵的一类，
    09-23/09-26 已按此判据接进 `testPath`/`testHeaders`/`extractScope`/`crawlForms`/`matchTitle`/`matchCode`/`matchRegexp` 等。
  - **调优参数类**（默认值 = 引擎内部兜底 ⇒ 不接不会造成假阴性）：如 `reqRate`/`compactErrorTemplates`/`timeBlindSamples`。
    仓库已在登记表里逐条写明「**有意不接**」及其理由，这是正确的处置。

**建议**：把登记表按这两类**分组显式化**，并把「能力缺失类」的条目数单独设一道只减不增的守卫。
现在两者混在一张 Set 里，缺口数一个数字看不出性质变化。

### 5. `pentest-lab` 的 `must` 大面积空转 `[读码]`

`e2e/pentest-lab/verify.mjs` 实测 **9 个场景写了 `must: []`**，其中 8 个属于空转：
`bl` / `enc` / `json_deep` / `waf403` / `empty` / `rdr` / `limit` / `mp`。
（第 9 个是 `safe`，它走 `expectSafe` 分支，判定为「必须 0 检出」，**不空转**。）

判定逻辑（`:98-106`）是 `ok = union.length > 0 && mustMissOk`，`must: []` ⇒ `mustMissOk` 恒真。
**后果**：这些场景里「某引擎退化到只剩 error 通道」照样 ✅。
这正是本仓反复踩的「**断言太浅 = 假绿**」，且已在 09-25 用同一手法修过 `multi-engine-lab`（把 `must` 接进判定）。

**顺序必须是「先修后钉」**（09-22 文档已写明）：直接钉 `bl: ['error','boolean','union']` 会让套件变红且无人敢动。
其中 `limit` 的 `must: []` 是**正确的**（`:40-44` 有取证：`LIMIT {n}` 只接受整数，union 语法上不成立），别当缺口修。

> 口径提醒：`grep -c "must: \[\]"` 得 10，其中 1 处是注释（`:40`）、1 处是 `safe`（走 `expectSafe`）
> ⇒ **真实空转场景是 8 个**。报数时别把这三个口径混起来。

### 6. P1-D 定库 sig 冲突 20 处仍未修 `[实测]`

```
$ node e2e/blackbox-lab/check-dbms-sig.mjs ; echo $?
1
  ClickHouse ⟷ H2, MySQL, Firebird, MonetDB
  H2 ⟷ MySQL, SQLite, ClickHouse, Firebird, MonetDB
  ...（共 20 处）
```

口径边界要守住（脚本头已写）：这是 **sig 层**冲突数，**不等于**运行时误判数 ——
冲突成立需「A 库的版本函数在 B 库上也能跑」。但历史上真踩过 H2 被抢、Sybase 被 `Release` 误命中。
**出路**：补一张跨库可执行性矩阵，把「理论冲突」收敛成「可执行冲突」。属纯工作量，不需外部条件。

### 7. 性能：真正的大头已被量化，且已被**有意搁置** `[实测]`

复跑 `npm run perf:budget`（记录型 stub 口径，无需靶场）：

| 检测器 | 请求数 | 归因 |
|---|---|---|
| error | **62** | 报错类 payload **45** + 其他变体 13 |
| union | **61** | `UNION SELECT` 试探 **52** + ORDER BY 8 |
| nosql / boolean / inline | 13 / 10 / 2 | — |
| **合计** | **148** | union + error = **83%** |

跨检测器「字节级完全相同」的请求**只有 1 条 × 6 份**（可省上界 3.4%）——
再次印证 09-23 那个「错了一个数量级」的自我推翻。

**关键**：这个大头**已经处理过了，结论是「不换」**。
`compactErrorTemplates` 开关已实现（`scanConfigGuard.js:307-311`、`defaults.js:274`），但**默认 false**，
理由写在登记表里：*「CI 实测裁剪会让 CRS PL1 技术位 8 → 6，本仓口径是『不用检出能力换请求数』」*。
→ **这条不该再当优化项推进**，除非产品定位变了。剩下可做的只有零风险项（C7 的 tamper 内 `new RegExp` 预编译）。

---

## 三、P2 · 结构性（大工程或受外部条件约束）

| # | 项 | 实测状态 | 说明 |
|---|---|---|---|
| 8 | **`Exploiter.js` 1005 行** | `wc -l` = 1005（`httpClient.js` 1115 / `Extractor.js` 884 / `ScanManager.js` 756） | 低于 1200 软上限，`arch-guard` 零告警。**已从「门禁驱动」降级为「机会性重构」**（09-23 结论）。注意 `httpClient.js` 仍是全仓最大的代码文件（1115 行） |
| 9 | **服务端 TS 化 0/360** | `find server/src -name "*.ts"` = **0** | 完全未开始。现状靠 `checkJs` 类型门禁兜，`tsc -p server/tsconfig.json` exit 0 |
| 10 | **真实 ModSecurity/Coraza 未实测** | `docker --version` → **exit 127** | 对外 WAF 可信度最大短板，所有数字仅在自实现执行器口径内成立。**本机无 docker，唯一出路是 CI 的 Linux runner** —— 而 CI 已恢复，这条路现在通了 |
| 11 | **桌面版无完整验收** | `e2e/release/release-smoke.mjs` 覆盖「引擎以生产配置启动 + API 启动扫描」，但**无安装 → 启动 → 握手 → 扫描 → 落盘五步一条 e2e** | 壳仅 210 行（`src-tauri/src/lib.rs` 206 + main.rs 4） |
| 12 | **文档体量本身是负担** | `CHANGELOG.md` **176 KB**（Unreleased 段 **2056 行**）、`TODO.md` 100 KB、`docs/` **48 个 md / 1.8 MB** | 本仓已多次踩「文档现状段系统性过期」；09-23 那份方案 10 项标「待做」其实已完成 |
| 13 | **根目录卫生** | 约 20 个日志文件 / 955 KB 堆在仓库根（`.qoder-*.log`/`.ci-e2e.log`/`tmp_j.log`/`ci-*.log`…），**全部 untracked**（`.gitignore` 的 `*.log` 覆盖了） | 不影响入库，但 `ls` 噪声大。属清洁度，非缺陷 |

**另需补的两条「已验证但未闭环」**（不是新缺口，是欠的验收口径）：
- **A1 收敛后的语义影响**：`stripEchoedPayload` 统一到强版后，三条链路（边界探测 / 布尔比对 / 定库）**多剔两个转义变体**
  → 「是否让判定变严、会不会漏检」**只有真靶场能证**，本地不申明已验证。
- **E2 的端到端**：`UI → REST → 引擎 → 真实拖库` 这条链路只验到「配置能穿过 REST 到达引擎」，
  真拖出数据只有靶场能证。CI 已恢复 ⇒ **这两条现在可以排期了**。

---

## 四、建议推进顺序

| 批次 | 内容 | 为什么排这里 |
|---|---|---|
| **1** | 收口在制品（§1）+ 前端覆盖率余量（§2） | 前者让 `facts:check` 回绿、恢复「可发布状态」；后者是**正在滑向刀尖的门禁**。归因已做完（真逻辑池 243 条 ≫ 所需 125 条）⇒ **走补测，不动阈值**，优先 `ValidityBanner.tsx`（可信度守卫的 UI 出口）。**§2b：补测已落地，覆盖率 88.73 → 90.97，余量 2.97pt，达标** |
| **2** | `getHeader` 收敛（§3）+ P1-D 可执行冲突矩阵（§6） | 纯静态、零外部依赖、改动小；错过它们的代价是静默的（与 09-23 批次 1 同源） |
| **3** | 靶场断言收紧（§5）+ 补 A1/E2 两条验收 | **CI 已恢复**，靶场类工作不再受阻 —— 这是本轮相对 09-23 最大的条件变化 |
| **4** | 真 ModSecurity 对拍（§10） | 唯一能救回对外 WAF 可信度的路。走 CI 的 Linux runner（`TODO.md` §P1-1 已有五步可执行步骤） |
| **5** | UI 键缺口分类治理（§4） | 收益最大但属产品决策，需先定「哪些算能力缺失」的清单 |
| **6** | `Exploiter.js` 拆分（§8）/ TS 化（§9） | 机会性，无阻塞；TS 化是跨季度工程 |

**每批落地时的验收口径**沿用本仓既有纪律：`文件:行号` + 可复跑命令（**退出码口径，勿用 `tail` 读**）+ 缺陷注入复验。

---

## 附 · 本次实测命令与退出码

```bash
npm run facts:check    # exit 1（采集源 323→325 文件）
npm run refs:check     # exit 0
npm run targets:check  # exit 0
npm run arch:guard     # exit 0
node scripts/readme-consistency.mjs          # exit 0
node e2e/blackbox-lab/check-dbms-sig.mjs     # exit 1（sig 层 20 处冲突）
node e2e/perf/request-budget.probe.mjs       # 148 请求 / union+error=83%
docker --version                             # exit 127（不可用）
wc -l server/src/engine/Exploiter.js         # 1005
```

> ⚠️ 本文所有「已落地」结论来自**读码与门禁退出码**，非运行验证；
> 靶场战绩未在本次重跑，引用前请按上表复跑。
